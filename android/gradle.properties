org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
flutter.minSdkVersion=16
flutter.targetSdkVersion=32
# device_info_plus (pulled in transitively via vibration) still applies the
# Kotlin Gradle Plugin directly, so built-in Kotlin can't be enabled yet.
# See: https://docs.flutter.dev/release/breaking-changes/migrate-to-built-in-kotlin/for-app-developers
android.builtInKotlin=false
android.newDsl=false
